# Server Configuration
PORT=4000

# Database Configuration
# SQLite database file path (default: ./employee_management.db)
DATABASE_PATH=./employee_management.db

# PostgreSQL Configuration (for future use)
# Uncomment and configure if switching to PostgreSQL
# DATABASE_URL=postgresql://user:password@localhost:5432/employee_management
